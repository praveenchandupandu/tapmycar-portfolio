# Node dependencies (Vercel installs these on deploy — never commit them)
node_modules/

# Environment variables (NEVER commit secrets)
.env
.env.local
.env.*.local

# OS-specific files
.DS_Store
Thumbs.db
desktop.ini

# Editor folders
.vscode/
.idea/

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vercel local cache
.vercel/

# Archived old patch scripts (kept locally for reference, not needed in repo)
archive/

tapmycar-logo-assets/
tapmycar-logo-assets.zip

# Patch backup folders (auto-generated by patch scripts)
backup-*/

# TMC_PATCH67 — Capacitor / native build artifacts
node_modules/
android/build/
android/.gradle/
android/app/build/
android/app/release/
android/local.properties
android/.idea/
android/*.iml
ios/App/build/
ios/App/Pods/
ios/App/Podfile.lock
ios/DerivedData/
ios/.xcode.env.local
.DS_Store

# Local-only legacy artifacts (TapMyCar)
tapmycar-patch*.js
backup-*/


# Local patch scripts (run-once tools, not part of the app)
patch-*.js
*.bak-*

